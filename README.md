# The-7
For Unit testing:
    Configure the project using:
        cmake -S System -B build

    Build the project using:
        cmake --build build

    Run the tests using:
        cd build
        ctest
